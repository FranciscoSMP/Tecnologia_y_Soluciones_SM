{{!-- ✅ Mensaje de éxito con animación --}}
{{#if success_msg}}
<div id="success-alert" class="alert-overlay fade-out">
  <div class="alert alert-success custom-alert text-center">
    {{success_msg}}
  </div>
</div>
{{/if}}

<div class="container my-5">

  <!-- 🧭 Encabezado con título y botón -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="text-center flex-grow-1">Listado de Usuarios</h2>
    <a href="/usuario/agregar" class="btn btn-success btn-sm ms-3">
      <i class="bi bi-plus-circle"></i> Nuevo Usuario
    </a>
  </div>

  {{#if usuarios.length}}
  <div class="table-responsive shadow-sm">
    <table class="table custom-table table-striped align-middle text-center">
      <thead class="table-success">
        <tr>
          <th>Nombre de Usuario</th>
          <th>Primer Nombre</th>
          <th>Segundo Nombre</th>
          <th>Primer Apellido</th>
          <th>Segundo Apellido</th>
          <th>Correo Electrónico</th>
          <th>Rol</th>
          <th class="text-center">Acciones</th>
        </tr>
      </thead>
      <tbody>
        {{#each usuarios}}
        <tr>
          <td>{{this.Nombre_Usuario}}</td>
          <td>{{this.Primer_Nombre}}</td>
          <td>{{this.Segundo_Nombre}}</td>
          <td>{{this.Primer_Apellido}}</td>
          <td>{{this.Segundo_Apellido}}</td>
          <td>{{this.Correo_Electronico}}</td>
          <td>
            {{#ifCond this.Nombre_Rol '==' 'Administrador'}}
            <span class="badge bg-primary">Administrador</span>
            {{else ifCond this.Nombre_Rol '==' 'Empleado'}}
            <span class="badge bg-secondary">Empleado</span>
            {{else}}
            <span class="badge bg-info text-dark">{{this.Nombre_Rol}}</span>
            {{/ifCond}}
          </td>
          <td>
            <a href="/usuario/editar/{{this.Id_Usuario}}" class="btn btn-sm btn-warning me-2">
              <i class="bi bi-pencil-square"></i> Editar
            </a>
            <button type="button" class="btn btn-sm btn-danger" data-bs-toggle="modal"
              data-bs-target="#confirmDeleteModal"
              onclick="setDeleteModal('Usuario', '{{this.Nombre_Usuario}}', 'usuario', {{this.Id_Usuario}})">
              <i class="bi bi-trash"></i> Eliminar
            </button>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{else}}
  <div class="alert alert-warning text-center shadow-sm">
    No hay usuarios registrados en este momento.
  </div>
  {{/if}}

  <!-- 🔙 Botón volver -->
  <a href="/dashboard" class="btn btn-secondary mt-3">
    <i class="bi bi-arrow-left-circle"></i> Volver al Panel
  </a>
</div>